{% extends 'base.html.twig' %}

{% block title %}ExamItem{% endblock %}

{% block body %}
    <h1>ExamItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exam_item.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ exam_item.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ exam_item.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exam_item_index') }}">back to list</a>

    <a href="{{ path('app_exam_item_edit', {'id': exam_item.id}) }}">edit</a>

    {{ include('exam_item/_delete_form.html.twig') }}
{% endblock %}

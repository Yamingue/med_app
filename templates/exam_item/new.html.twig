{% extends 'main.html.twig' %}

{% block title %}New ExamItem{% endblock %}

{% block body %}

<div class="row">
    <div class="col-sm-6 mx-auto">
        <div class="card">
            <div class="card-header">
                <a class="btn btn-sm btn-primary" href="{{ path('app_exam_item_index') }}">
                    {% trans %}Retour{% endtrans %}
                </a>
            </div>
            <div class="card-body">
                {{ include('exam_item/_form.html.twig') }}
            </div>
        </div>
    </div>
</div>


{% endblock %}
{% extends 'main.html.twig' %}

{% block title %}
{% trans %}Editer Medicament{% endtrans %}
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-sm-6 mx-auto">
        <div class="card">
            <div class="card-header">
                <div class="d-flex justify-content-between">
                    <a class="btn btn-primary" href="{{ path('app_admin_medicament_controlleur_index') }}">
                        {% trans %}Retour a la list{% endtrans %}
                    </a>

                    {{ include('admin_medicament_controlleur/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="card-body">
                {{ include('admin_medicament_controlleur/_form.html.twig', {'button_label': 'Modifer'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% extends 'main.html.twig' %}

{% block title %}Detail{% endblock %}

{% block body %}
<a class="btn btn-primary" href="{{ path('app_type_consultation_index') }}">
    {% trans %}Retour{% endtrans %}
</a>
<div class="card mt-2">
    <div class="card-body">
        <table class="table">
            <tbody>
                {# <tr>
                    <th>{% trans %}Id{% endtrans %}</th>
                    <td>{{ type_consultation.id }}</td>
                </tr> #}
                <tr>
                    <th>{% trans %}Nom{% endtrans %}</th>
                    <td>{{ type_consultation.nom }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>



<div class="d-flex justify-content-between mt-2">
    <a class="btn btn-primary" href="{{ path('app_type_consultation_edit', {'id': type_consultation.id}) }}">
        {% trans %}Editer{% endtrans %}
    </a>

    {{ include('type_consultation/_delete_form.html.twig') }}
</div>
{% endblock %}